@*@model Tamin.Models.PostGroup

    @using (Ajax.BeginForm("Add", "AdminPostGroups", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "div-record", OnSuccess = "$('.close').click()" }))

    {

        <div class="modal-body">

            <fieldset>

                <div class="form-group">
                    @Html.LabelFor(model => model.ParentId, "والد گروه خبر", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.DropDownList("ParentId", "-- والد گروه خبر را انتخاب کنید --")
                        @Html.ValidationMessageFor(model => model.ParentId)
                    </div>
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.PostGroupTitle, new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.PostGroupTitle)
                        @Html.ValidationMessageFor(model => model.PostGroupTitle)
                    </div>
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.ImageUrl, new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.ImageUrl)
                        @Html.ValidationMessageFor(model => model.ImageUrl)
                    </div>
                </div>

                <div id="buttons">
                    <div class="col-md-2">
                    </div>
                    <div class="col-md-10">
                        <input type="submit" class="btn btn-primary" value="ثبت" />
                        @Html.ActionLink("بازگشت به فهرست", "Index")
                    </div>
                </div>
            </fieldset>


        </div>

        <div class="modal-footer">

            <button type="button" class="btn btn-default" data-dismiss="modal"><i class="fa fa-times"></i>&nbsp;Close</button>

            <button type="submit" class="btn btn-primary" name="cmd"><i class="fa fa-check"></i>&nbsp;Save</button>

        </div>

    }*@

@model Tamin.Models.PostGroup
@{

    //ViewBag.Title = "ADD";
    //Layout = Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    //Layout = Layout = "~/Views/Shared/_LayoutAdmin - Copy.cshtml";
    //modal - backdrop
    Layout = null;
    //using (Html.BeginForm("Add", "AdminPostGroups", FormMethod.Post, new { @id = "form_UploadFile", @enctype = "multipart/form-data" }))

}
@*@using (Html.BeginForm("Add", "AdminPostGroups", new { @id = "form_UploadFile", @enctype = "multipart/form-data" }, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "div-record", OnSuccess = "$('.close').click()" }))*@
@*@using (Html.BeginForm("Add", "AdminPostGroups", FormMethod.Post, new { @id = "form_UploadFile", @enctype = "multipart/form-data" }))*@
@*@using (Ajax.BeginForm("Add", "AdminPostGroups", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "div-record", OnSuccess = "$('.close').click()" }, new { @id = "form_UploadFile", @enctype = "multipart/form-data" }))*@
@using (Ajax.BeginForm("Add", "AdminPostGroups", new AjaxOptions
{
    HttpMethod = "Post",
    UpdateTargetId = "div-record",
    OnSuccess = "$('.close').click()"
}, new { @id = "form_UploadFile", @enctype = "multipart/form-data" }))

{

    <div class="modal-body">

        <div class="row form-group">

            <div class="col-md-12">

                <div class="input-group">



                    @Html.TextBoxFor(m => m.PostGroupTitle, new { @class = "form-control", @placeholder = "عنوان گروه خبر" })

                </div>

            </div>

        </div>

        @*<div class="row form-group">

                <div class="col-md-12">

                    <div class="input-group">

                        <span class="input-group-addon"><i class="glyphicon glyphicon-calendar"></i></span>

                        <div class="form-group">
                            @Html.LabelFor(model => model.ParentId, "والد گروه خبر", new { @class = "control-label col-md-2" })
                            <div class="col-md-10">
                                @Html.DropDownList("ParentId", "-- والد گروه خبر را انتخاب کنید --")
                                @Html.ValidationMessageFor(model => model.ParentId)
                            </div>
                        </div>

                    </div>

                </div>

            </div>*@
        <br />

        <div class="row form-group">

            <div class="col-md-12">

                <div class="input-group">



                    @Html.LabelFor(model => model.ImageUrl, new { @class = "control-label col-md-4 right" })
                    <br />
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.ImageUrl)
                        @Html.ValidationMessageFor(model => model.ImageUrl)

                    </div>

                </div>

            </div>
        </div>


    </div>

    <div class="modal-footer">
        <button type="submit" class="btn btn-primary" name="cmd"><i class="fa fa-check"></i>&nbsp;ذخیره</button>
        <button type="button" class="btn btn-default" data-dismiss="modal"><i class="fa fa-times"></i>&nbsp;بستن</button>
    </div>

}